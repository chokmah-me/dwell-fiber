# Workplan — week of 2026-09-28

Where things stand: V3 calibration is feasible for the first time
(`P_b = 0.0`, `P_i = 681.37` → throttle 102.2, kill 204.4, all gates pass),
the BPF verifier bug is fixed, and the ACP policy shows a ~2× live
attack-peak lift vs the fixed-pricing control. Thresholds are **not** locked
into daemon defaults yet. Items are ordered by priority; (WSL) marks work
that needs the BPF-capable host.

## 1. Identify the ambient enumeration source (WSL) — blocks threshold lock-in

Open since calibration pass 1. ~30 s cadence bursts: UFM 200–679/s, TBW ≈ 0,
PIDs with odd comms (`(9)`, `cloud-id`, `bash`). Post-measurement ambient
peaked ~96 vs throttle 102.2 — too thin to lock thresholds against.

- Correlate burst times in the daemon log with `ps`/systemd timers
  (`apt-daily`, `updatedb`, snap refresh, WSL interop processes).
- Done when: the source is named and its price ceiling is measured with
  comfortable margin below the throttle, or the budget is adjusted to create
  that margin.

## 2. Record poller time series (small code change, big payoff)

`test/calibrate_v3.py --from-metrics` currently keeps only peak/final, so
time-to-throttle (the live analog of the sim's 24.5 → 9.0 window detection
delay) cannot be computed. Write the samples to the poller JSON.

- Done when: both arms' price curves are plottable and time-to-102.2 is
  reported for control vs ACP.

## 3. Exercise the recon/learning phases live (WSL)

The live runs locked the estimator onto exploitation immediately; the
recon/learning dampening multipliers (×0.4/×0.8) are unvalidated. Build a
slow-enumeration workload (many distinct inodes, low rate, long dwell —
recon-like) and confirm the estimator classifies it as recon and the price
stays damped.

- Done when: a recon workload shows phase=1 in `dwell_fiber_v3_acp_phase`
  and suppressed pricing vs the same workload under fixed pricing.

## 4. Ambient-vs-enumeration distinguisher (design)

The sim's open caveat, confirmed live: recon dampening also slows the
ambient contaminant's price climb — the policy cannot tell a real
enumeration from an ambient open storm. Candidate signals: TBW ≈ 0 with no
write syscalls, parent-process reputation, per-PID historical baseline.

- Done when: a discriminator is specified (doc) and ideally prototyped
  behind a flag.

## 5. Lock V3ThrottlePrice/V3KillPrice defaults

After (1): set daemon defaults (currently starting points) to the calibrated
band — or the adjusted band if the ambient work moves it — via flags/config.
Keep observation-mode as the default until (6) is proven.

## 6. V3 enforcement dry-run → armed test (WSL)

`--v3-enforce` exists but has never been armed against a live attack. After
thresholds are locked: arm throttling (not killing) on the WSL guest during
the intermittent bench and confirm the attack is throttled while benign tar
is untouched.

## 7. Cosmetic: `(9)` comm fallback

Unrecognized process names fall back to tier T2. Investigate the WSL comm
source for these PIDs (truncated `bpf_get_current_comm`? kernel threads?) and
decide whether the T2 default is the right fallback.

## Out of scope for this week

- New audit targets, paid services, repo-visibility changes.
- Locking thresholds before the ambient source is identified (fail-closed).
