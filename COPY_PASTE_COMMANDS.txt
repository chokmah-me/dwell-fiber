#!/bin/bash
# EXACT COMMANDS TO RUN - Copy and paste these

cat << 'EOF'
╔════════════════════════════════════════════════════════════════════════════╗
║           EXACT COMMANDS TO COPY & PASTE                                  ║
║                    Push to GitHub + VM Setup                              ║
╚════════════════════════════════════════════════════════════════════════════╝

═══════════════════════════════════════════════════════════════════════════════
STEP 1: PUSH TO GITHUB FROM YOUR WINDOWS MACHINE
═══════════════════════════════════════════════════════════════════════════════

Open PowerShell and run these exact commands:

  cd c:\Users\danie\dwell-fiber-1
  git log --oneline -5
  git status

You should see:
  - 2 commits ahead of origin/main
  - working tree clean

Then authenticate and push:

  # Option A: Using GitHub CLI (easiest)
  gh auth login
  # Follow prompts to authenticate
  git push origin main

  # Option B: Using SSH (if you have SSH key set up)
  git push origin main

  # Option C: Using Personal Access Token
  # Create token at: https://github.com/settings/tokens
  git push origin main
  # When prompted for password, paste the token

After push succeeds, you should see:
  [main 24021a9] docs: Add VM setup...
  ...
  To github.com:dyb5784/dwell-fiber.git
  ✓ Branch main set up to track remote branch main from origin.

═══════════════════════════════════════════════════════════════════════════════
STEP 2: QUICK SETUP ON YOUR UBUNTU 25.10 VM
═══════════════════════════════════════════════════════════════════════════════

Run these commands in your VM terminal:

  # One-liner to run automated setup
  cd $HOME && \
  wget https://raw.githubusercontent.com/dyb5784/dwell-fiber/main/vm-setup.sh && \
  bash vm-setup.sh

This does everything:
  ✓ Updates system
  ✓ Installs dependencies
  ✓ Fixes Ubuntu 25.10 asm symlink
  ✓ Clones/updates repo
  ✓ Builds everything
  ✓ Verifies build

Expected output: "SETUP COMPLETE! Ready for testing"

═══════════════════════════════════════════════════════════════════════════════
STEP 3: START TESTING ON VM
═══════════════════════════════════════════════════════════════════════════════

After setup completes, open 4 terminals on your VM:

Terminal 1 - Start daemon in simulation:
  cd ~/dwell-fiber
  ./bin/dwell-fiber-daemon --simulate

Terminal 2 - Monitor metrics:
  watch -n 1 'curl -s http://localhost:9090/metrics | grep dwell_fiber'

Terminal 3 - Generate workload:
  cd ~/dwell-fiber/test
  go run workload_generator.go

Terminal 4 - Open dashboard (on your host machine or VM with GUI):
  firefox http://localhost:9090

You should see:
  ✓ Dashboard loads with dwell time
  ✓ Price increasing/decreasing
  ✓ Status changes (green/yellow/red)
  ✓ Workload completing with "✓ operations"

═══════════════════════════════════════════════════════════════════════════════
STEP 4: TEST REAL BPF MODE (Optional, requires root)
═══════════════════════════════════════════════════════════════════════════════

Terminal 1 - Start with real BPF:
  cd ~/dwell-fiber
  sudo ./bin/dwell-fiber-daemon --alpha=0.5 --budget=5.0

Terminal 2 - Generate workload:
  cd ~/dwell-fiber/test
  go run workload_generator.go

Terminal 3 - Watch for enforcement decisions:
  sudo tail -f /var/log/syslog | grep -i "high dwell"

You should see:
  high dwell: PID=12345 (bash) dwell=7.00s
  high dwell: PID=12346 (bash) dwell=9.00s

═══════════════════════════════════════════════════════════════════════════════
STEP 5: RUN FULL INTEGRATION TEST
═══════════════════════════════════════════════════════════════════════════════

Single command to test everything:

  cd ~/dwell-fiber
  chmod +x test/test-suite.sh
  ./test/test-suite.sh

Expected output:
  ✓ Build: Success
  ✓ Daemon: Running
  ✓ Workload: Generated
  ✓ Metrics: Available
  → Open: http://localhost:9090

═══════════════════════════════════════════════════════════════════════════════
QUICK COMMANDS FOR VM (After setup)
═══════════════════════════════════════════════════════════════════════════════

Build daemon:
  cd ~/dwell-fiber && make daemon

Start simulation:
  ./bin/dwell-fiber-daemon --simulate

Start real BPF:
  sudo ./bin/dwell-fiber-daemon

Generate workload:
  cd ~/dwell-fiber/test && go run workload_generator.go

Check metrics:
  curl http://localhost:9090/metrics

Pull latest changes:
  cd ~/dwell-fiber && git pull origin main

View guides:
  cat ~/dwell-fiber/TESTING.md
  cat ~/dwell-fiber/test/QUICK_REFERENCE.sh
  cat ~/dwell-fiber/TROUBLESHOOTING.md

═══════════════════════════════════════════════════════════════════════════════
VERIFICATION CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

After completing all steps, verify:

On Windows:
  [ ] git push origin main (succeeded)
  [ ] Both commits visible on GitHub

On VM:
  [ ] ./bin/dwell-fiber-daemon --simulate (starts)
  [ ] curl http://localhost:9090/metrics (responds)
  [ ] firefox http://localhost:9090 (dashboard loads)
  [ ] cd test && go run workload_generator.go (completes)
  [ ] Dashboard shows dwell time 5-9 seconds
  [ ] Dashboard shows price > 0.1
  [ ] ./test/test-suite.sh (passes)

If all checks pass: ✅ SYSTEM WORKING PERFECTLY

═══════════════════════════════════════════════════════════════════════════════
TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

If push fails on Windows:
  git status  # Check what's there
  git remote -v  # Verify remote
  gh auth login  # Authenticate
  git push origin main  # Try again

If VM setup fails:
  sudo ln -sf /usr/include/x86_64-linux-gnu/asm /usr/include/asm
  make clean all
  ./bin/dwell-fiber-daemon --simulate

If no events appear:
  cd ~/dwell-fiber/test && go run workload_generator.go
  tail -f /var/log/syslog | grep "high dwell"

If dashboard doesn't load:
  curl http://localhost:9090
  ps aux | grep dwell-fiber

For complete troubleshooting:
  cat ~/dwell-fiber/TROUBLESHOOTING.md

═══════════════════════════════════════════════════════════════════════════════
SUCCESS INDICATORS
═══════════════════════════════════════════════════════════════════════════════

Windows:
  ✅ git push origin main (no errors)
  ✅ GitHub shows 2 new commits

VM (After vm-setup.sh):
  ✅ "SETUP COMPLETE! Ready for testing"
  ✅ bin/dwell-fiber-daemon exists
  ✅ ./bin/dwell-fiber-daemon --simulate (starts)

Testing:
  ✅ Dashboard loads at http://localhost:9090
  ✅ Shows dwell time value
  ✅ Shows price value
  ✅ Updates in real-time
  ✅ Workload completes successfully

═══════════════════════════════════════════════════════════════════════════════

That's it! You're done! 🎉

Next: 
  1. Run the Windows push commands
  2. Run the VM setup script
  3. Start testing with the 4-terminal setup
  4. Open dashboard in Firefox

Happy testing! 🛡️
EOF
